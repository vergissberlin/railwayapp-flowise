# Optional but recommended — set in Railway dashboard as generated secrets to enable Basic Auth
FLOWISE_USERNAME=replace-with-username
# Optional but recommended — set in Railway dashboard as generated secrets to enable Basic Auth
FLOWISE_PASSWORD=replace-with-strong-password
